# Vercel Postgres 데이터베이스 연결 정보
# Vercel 대시보드에서 Postgres를 추가하면 자동으로 설정됩니다.
POSTGRES_URL="postgres://username:password@host:5432/database"
POSTGRES_PRISMA_URL="postgres://username:password@host:5432/database?pgbouncer=true"
POSTGRES_URL_NON_POOLING="postgres://username:password@host:5432/database"
POSTGRES_USER="username"
POSTGRES_HOST="host"
POSTGRES_PASSWORD="password"
POSTGRES_DATABASE="database"

# 관리자 인증 토큰 (임의의 긴 문자열 생성)
# 예: openssl rand -hex 32
ADMIN_TOKEN="your-secure-admin-token-here"

# 텔레그램 알림 설정
# 1. @BotFather에게 /newbot 명령으로 봇 생성
# 2. 봇 토큰을 받아서 설정
TELEGRAM_BOT_TOKEN="1234567890:ABCdefGHIjklMNOpqrsTUVwxyz"
# 3. 봇과 대화를 시작한 후 채팅 ID 확인
# 4. https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates 에서 chat.id 확인
TELEGRAM_CHAT_ID="123456789"

# 이메일 알림 설정 (선택사항)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-email@gmail.com"
SMTP_PASSWORD="your-app-password"
ADMIN_EMAIL="hazacheck@gmail.com"